##### .env.example (回退版本示例) #####
# 复制为 .env 后使用。若需启用多模态，将 DISABLE_VISION 设为 0 并按需调参。

# === 基础模型配置 ===
OPENAI_API_KEY=""                # 必填：你的 API Key
OPENAI_BASE_URL="https://api.deepseek.com"  # DeepSeek 示例；OpenAI 可留空

# 纯文本模型（测试用例 / 测试方案）
TEXT_MODEL_NAME="deepseek-chat"
# 视觉模型占位（当前回退逻辑不使用，保留便于未来切换）
VISION_MODEL_NAME="deepseek-chat"

# 多模态载荷风格：auto | openai | deepseek | text-only
VISION_PAYLOAD_STYLE="auto"

# === 回退总开关 ===
# 1=禁用图片理解与分批，直接走纯文本路径；0=按下方图片参数启用（需自行恢复原多模态逻辑）
DISABLE_VISION=1

# === 图片与分批参数（仅在 DISABLE_VISION=0 时生效） ===
MAX_IMAGE_DIM=1024
JPEG_QUALITY=72
MAX_TOTAL_IMAGE_DATA_CHARS=5000000
MAX_IMAGES_PER_BATCH=12
PARALLEL_BATCH=0
BATCH_PARALLEL_WORKERS=3
BATCH_RETRY_MAX=2
BATCH_RETRY_BACKOFF_BASE=0.6
MAX_FIRST_BATCH_CHARS=60000
IMAGE_ENCODING_MODE=data-url      # data-url | url | skip

# === 规范化后处理 ===
FINAL_NORMALIZE_PLAN=0

# === 其他（可选） ===
LOG_LEVEL=info
CACHE_MAX_ITEMS=200

# 提示：将此文件重命名为 .env 并填入 OPENAI_API_KEY 即可运行。若需重新启用视觉，请设置 DISABLE_VISION=0。
